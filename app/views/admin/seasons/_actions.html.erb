<ul>
  <% if Admin::SeasonsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_seasons_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_season_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::SeasonsController.sortable? and Season.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Seasons"),
                 admin_seasons_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Seasons"),
                 admin_seasons_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>